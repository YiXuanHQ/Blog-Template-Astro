---
title: 坐标轴设置
prevChapter: "python-ai/matplotlib/03-图表样式"
nextChapter: "python-ai/matplotlib/05-统计图表"
parentChapter: "python-ai/matplotlib/README"
---
# 坐标轴设置

> 掌握坐标轴的精细控制

## 📚 学习目标

- 掌握坐标轴范围设置
- 学会刻度配置
- 掌握网格线设置
- 学会双坐标轴

## 1. 坐标轴范围

```python
import matplotlib.pyplot as plt
import numpy as np

x = np.linspace(0, 10, 100)
y = np.sin(x)

fig, ax = plt.subplots()
ax.plot(x, y)

# 设置范围
ax.set_xlim(0, 10)
ax.set_ylim(-1.5, 1.5)

# 自动范围
ax.autoscale()

plt.show()
```

## 2. 刻度设置

```python
# 设置刻度位置
ax.set_xticks([0, 2, 4, 6, 8, 10])
ax.set_yticks([-1, 0, 1])

# 设置刻度标签
ax.set_xticklabels(['A', 'B', 'C', 'D', 'E', 'F'])

# 刻度格式化
from matplotlib.ticker import FuncFormatter
ax.yaxis.set_major_formatter(FuncFormatter(lambda y, _: f'{y:.2f}'))
```

## 3. 网格线

```python
ax.grid(True)  # 显示网格
ax.grid(axis='x')  # 只显示x轴网格
ax.grid(color='gray', linestyle='--', linewidth=0.5, alpha=0.5)
```

## 4. 双坐标轴

```python
fig, ax1 = plt.subplots()

ax1.plot(x, y1, 'b-')
ax1.set_ylabel('Y1', color='b')

ax2 = ax1.twinx()
ax2.plot(x, y2, 'r-')
ax2.set_ylabel('Y2', color='r')

plt.show()
```

---

**下一节：** [统计图表](05-统计图表.md)
