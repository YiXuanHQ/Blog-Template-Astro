---
title: 数据库的备份与恢复
prevChapter: "java-backend/mysql/第04章-实践篇/06-数据库的安全管理"
parentChapter: "java-backend/mysql/README"
---
# 数据库的备份与恢复

> 本章通过实际案例学习MySQL数据库的备份与恢复方法，包括完全备份、增量备份、使用mysqldump、SELECT INTO OUTFILE、二进制日志恢复等。掌握数据库备份和恢复的完整流程。

## ⚡ 快速参考

- **备份工具**：mysqldump（逻辑备份）、物理备份（文件复制）
- **备份类型**：完全备份、增量备份（binlog）
- **导出数据**：SELECT INTO OUTFILE、mysqldump
- **导入数据**：LOAD DATA INFILE、mysqlimport、source命令
- **二进制日志**：mysqlbinlog查看、基于时间点恢复、基于位置恢复
- **备份策略**：定期备份、备份验证、备份存储、恢复测试

## 📚 学习目标

1. 掌握使用mysqldump进行数据库备份的方法
2. 理解完全备份和增量备份的区别
3. 掌握使用mysql命令和source命令恢复备份
4. 掌握SELECT INTO OUTFILE导出数据的方法
5. 掌握LOAD DATA INFILE和mysqlimport导入数据
6. 理解二进制日志的作用和使用方法
7. 掌握基于时间点和位置的数据库恢复方法
8. 能够制定合理的数据库备份和恢复策略

## 实践目标

通过本实践，你将学会：
- ✅ 使用mysqldump进行完全备份
- ✅ 使用mysql命令恢复备份
- ✅ 使用SELECT INTO OUTFILE导出数据
- ✅ 使用LOAD DATA INFILE导入数据
- ✅ 使用mysqlimport导入数据
- ✅ 使用二进制日志进行增量备份和恢复

## 实践环境

**数据库：** studentsdb

**数据表：**
- `student_info`：学生信息表
- `curriculum`：课程表
- `grade`：成绩表

**备份目录：** 根据实际情况设置（Windows示例：`D:\`，Linux示例：`/tmp/`）

## 实践步骤

### 一、mysqldump备份与恢复

#### 1. 完全备份数据库

使用 `mysqldump` 命令备份数据库 `studentsdb` 的所有表，存于 `D:\` 下，文件名为 `all_tables.sql`。

**Windows环境：**
```bash
# 先转到MySQL的bin文件夹
cd C:\Program Files\MySQL\MySQL Server 8.0\bin

# 执行备份命令
mysqldump -u root -h localhost -p studentsdb > d:\all_tables.sql
```

**Linux环境：**
```bash
mysqldump -u root -h localhost -p studentsdb > /tmp/all_tables.sql
```

**参数说明：**
- `-u root`：指定用户名
- `-h localhost`：指定主机
- `-p`：提示输入密码
- `studentsdb`：要备份的数据库名
- `>`：重定向输出到文件

#### 2. 恢复数据库

在MySQL服务器上创建数据库 `student1`，使用 `mysql` 命令将备份文件 `all_tables.sql` 恢复到数据库 `student1` 中。

**（1）创建数据库**
```sql
CREATE DATABASE student1;
```

**（2）恢复备份（Windows环境）**
```bash
# 在CMD命令提示符窗口中执行
cd C:\Program Files\MySQL\MySQL Server 8.0\bin
mysql -u root -p student1 < d:\all_tables.sql
```

**（3）恢复备份（Linux环境）**
```bash
mysql -u root -p student1 < /tmp/all_tables.sql
```

**验证恢复：**
```sql
USE student1;
SHOW TABLES;
SELECT * FROM student_info;
```

#### 3. 备份指定表

使用 `mysqldump` 命令备份数据库 `studentsdb` 的 `student_info` 和 `curriculum` 表，存于 `D:\` 下，文件名为 `s_c.sql`。

**Windows环境：**
```bash
mysqldump -u root -h localhost -p studentsdb student_info curriculum > d:\s_c.sql
```

**Linux环境：**
```bash
mysqldump -u root -h localhost -p studentsdb student_info curriculum > /tmp/s_c.sql
```

#### 4. 恢复指定表

在MySQL服务器上创建数据库 `student2`，使用 `mysql` 命令将备份文件 `s_c.sql` 恢复到数据库 `student2` 中。

**（1）创建数据库**
```sql
CREATE DATABASE student2;
```

**（2）恢复备份**
```bash
# Windows环境
mysql -u root -p student2 < d:\s_c.sql

# Linux环境
mysql -u root -p student2 < /tmp/s_c.sql
```

### 二、数据导出和导入

#### 5. 使用mysqldump导出到文本文件

使用 `mysqldump` 命令将 `studentsdb` 数据库中的 `grade` 表中的记录导出到文本文件。

**重要提示：** 在使用 `SELECT ... INTO OUTFILE` 语句或 `mysqldump -T` 时，目标文件的路径只能是MySQL的 `secure_file_priv` 参数所指定的位置。

**查看secure_file_priv设置：**
```sql
SELECT @@secure_file_priv;
-- 或
SHOW VARIABLES LIKE 'secure%';
```

**如果没有设置，需要修改MySQL配置文件：**
```ini
# my.cnf 或 my.ini
[mysqld]
secure_file_priv = "C:/Users/YourName/Desktop/mysql-uploads"
# Linux示例：secure_file_priv = "/tmp/mysql-uploads"
```

**导出数据（Windows环境示例）：**
```bash
mysqldump -u root -p -T "C:\Users\zjj\Desktop\mysql-uploads" studentsdb grade --lines-terminated-by=\r\n
```

**导出结果：**
- `grade.sql`：表结构文件
- `grade.txt`：数据文件

#### 6. 使用mysqlimport导入数据

删除数据库 `student1` 的 `grade` 表中全部记录，使用 `mysqlimport` 命令将 `grade.txt` 文件中的数据导入到 `student1` 的 `grade` 表中。

**（1）删除数据**
```sql
USE student1;
SET SQL_SAFE_UPDATES = 0;
DELETE FROM grade;
SET SQL_SAFE_UPDATES = 1;
```

**（2）导入数据（Windows环境示例）**
```bash
mysqlimport -u root -p student1 "C:\Users\zjj\Desktop\mysql-uploads\grade.txt" --lines-terminated-by=\r\n
```

**（3）验证导入**
```sql
USE student1;
SELECT * FROM grade;
```

#### 7. 使用SELECT INTO OUTFILE导出数据

使用 `SELECT ... INTO OUTFILE` 语句备份 `studentsdb` 数据库中的 `curriculum` 表的数据到文本文件 `c.txt`。要求字段之间用"|"隔开，字符型数据用双引号括起来。

```sql
USE studentsdb;

SELECT * FROM curriculum
INTO OUTFILE 'C:/Users/zjj/Desktop/mysql-uploads/c.txt'
FIELDS TERMINATED BY '|'
OPTIONALLY ENCLOSED BY '"'
LINES TERMINATED BY '\r\n';
```

**参数说明：**
- `FIELDS TERMINATED BY`：字段分隔符
- `OPTIONALLY ENCLOSED BY`：字段包围符
- `LINES TERMINATED BY`：行结束符

#### 8. 使用LOAD DATA INFILE导入数据

删除数据库 `student1` 的 `curriculum` 表中全部记录，使用 `LOAD DATA INFILE` 语句将 `c.txt` 文件中的数据导入到 `student1` 的 `curriculum` 表中。

**（1）删除数据**
```sql
USE student1;
SET SQL_SAFE_UPDATES = 0;
DELETE FROM curriculum;
SET SQL_SAFE_UPDATES = 1;
```

**（2）导入数据**
```sql
LOAD DATA INFILE 'C:/Users/zjj/Desktop/mysql-uploads/c.txt'
INTO TABLE student1.curriculum
FIELDS TERMINATED BY '|'
OPTIONALLY ENCLOSED BY '"'
LINES TERMINATED BY '\r\n';
```

**（3）验证导入**
```sql
SELECT * FROM student1.curriculum;
```

### 三、二进制日志备份与恢复

#### 9. 使用二进制日志恢复数据库

**（1）完全备份数据库**

使用 `mysqldump` 命令备份所有数据库到 `D:\` 的 `all_db.sql`。

```bash
# Windows环境
mysqldump -u root -h localhost -p --single-transaction --flush-logs --master-data=2 --all-databases > d:\all_db.sql

# Linux环境
mysqldump -u root -h localhost -p --single-transaction --flush-logs --master-data=2 --all-databases > /tmp/all_db.sql
```

**参数说明：**
- `--single-transaction`：使用事务保证一致性（InnoDB）
- `--flush-logs`：刷新日志，生成新的binlog文件
- `--master-data=2`：记录binlog位置信息
- `--all-databases`：备份所有数据库

**（2）删除数据（模拟数据丢失）**

删除 `studentsdb` 数据库 `student_info` 表的所有记录。

```sql
USE studentsdb;
SET SQL_SAFE_UPDATES = 0;
DELETE FROM student_info;
SET SQL_SAFE_UPDATES = 1;
```

**（3）进行增量备份**

使用 `mysqladmin` 进行增量备份（刷新日志）。

```bash
mysqladmin -u root -h localhost -p flush-logs
```

**（4）恢复完全备份**

使用 `mysql` 命令恢复 `all_db.sql` 文件的完全备份。

```bash
# Windows环境
mysql -u root -p < d:\all_db.sql

# Linux环境
mysql -u root -p < /tmp/all_db.sql
```

**（5）查看二进制日志信息**

```sql
-- 查看当前数据库是否开启binlog
SHOW VARIABLES LIKE '%log_bin%';

-- 查看当前数据库binary log的位置
SHOW MASTER STATUS;

-- 查看binlog日志文件有哪些
SHOW BINARY LOGS;

-- 查看具体日志文件
SHOW BINLOG EVENTS IN 'binlog.000001';
```

**（6）使用mysqlbinlog恢复增量备份**

使用 `mysqlbinlog` 命令恢复增量备份。

**Windows环境示例：**
```bash
mysqlbinlog "C:\ProgramData\MySQL\MySQL Server 8.0\Data\binlog.000001" | mysql -u root -p
```

**Linux环境示例：**
```bash
mysqlbinlog /var/lib/mysql/binlog.000001 | mysql -u root -p
```

**（7）验证恢复**

```sql
USE studentsdb;
SELECT * FROM student_info;
```

## 实践总结

### 关键知识点

#### 备份类型

1. **完全备份**
   - 备份整个数据库
   - 使用 `mysqldump`
   - 可以恢复到任意时间点（配合binlog）

2. **增量备份**
   - 备份binlog日志
   - 记录数据变更
   - 需要先有完全备份

3. **表级备份**
   - 备份指定表
   - 使用 `mysqldump 数据库名 表名`

#### 备份方法对比

| 方法 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| **mysqldump** | 简单、跨平台 | 速度较慢 | 小型数据库、逻辑备份 |
| **SELECT INTO OUTFILE** | 速度快 | 需要文件权限 | 数据导出 |
| **二进制日志** | 精确恢复 | 需要开启binlog | 增量备份、时间点恢复 |

### 注意事项

- ⚠️ 备份前确保数据库正常运行
- ⚠️ 定期测试备份文件的恢复能力
- ⚠️ `secure_file_priv` 参数限制文件导出位置
- ⚠️ 二进制日志需要定期清理
- ✅ 生产环境建议使用 `--single-transaction` 参数
- ✅ 备份文件要妥善保管，定期备份
- ✅ 恢复前先备份当前数据

### 最佳实践

1. **备份策略**
   - 完全备份：每天一次
   - 增量备份：每小时一次
   - 保留多个备份版本

2. **恢复测试**
   - 定期测试备份恢复
   - 验证数据完整性
   - 记录恢复时间

3. **安全措施**
   - 备份文件加密存储
   - 异地备份
   - 权限控制

---

## 练习题

1. 备份 `studentsdb` 数据库的所有表
2. 创建一个新数据库，恢复备份的数据
3. 导出 `grade` 表的数据到文本文件
4. 删除 `grade` 表的数据，然后从文本文件恢复
5. 配置二进制日志，进行增量备份和恢复测试

---

**上一章：** [数据库的安全管理](/tutorials/java-backend/mysql/第04章-实践篇/06-数据库的安全管理/)

**下一章：** [日志管理](/tutorials/java-backend/mysql/第05章-运维篇/01-日志管理/) →

**恭喜完成MySQL实践篇学习！** 🎉
